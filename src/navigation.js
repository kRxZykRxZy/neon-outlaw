import * as THREE from 'three';
const $=id=>document.getElementById(id);
let game=null;
function ensureUI(){if($('nav-hint'))return;const hint=document.createElement('div');hint.id='nav-hint';hint.className='nav-hint';document.body.appendChild(hint);const arrow=document.createElement('div');arrow.id='nav-arrow';arrow.className='nav-arrow';arrow.textContent='▲';document.body.appendChild(arrow)}
function targetFor(m){if(!game||!m)return null;if(['combat','boss'].includes(m.type)){const e=game.enemies?.enemies?.find(x=>!x.dead);if(e)return e.group.position}const centers={dock:[-520,1.2,-420],market:[0,1.2,-420],midtown:[460,1.2,-80],old:[-430,1.2,340],corporate:[350,1.2,430],outskirts:[-20,1.2,850]};const p=centers[m.district]||[0,1.2,0];return new THREE.Vector3(...p)}
function update(){game=window.game;if(!game||!game.ready||!game.player||!game.missions)return;ensureUI();const hint=$('nav-hint'),arrow=$('nav-arrow'),m=game.missions.active;if(!m){hint.textContent='FREE ROAM — explore the village and city';arrow.style.display='none';return}const t=targetFor(m);if(!t)return;const p=game.player.inCar?game.player.car.group.position:game.player.group.position;const dist=p.distanceTo(t);const dx=t.x-p.x,dz=t.z-p.z;const worldAngle=Math.atan2(dx,dz);const rel=worldAngle-game.player.cameraYaw;const deg=((rel*180/Math.PI+540)%360)-180;arrow.style.display='block';arrow.style.transform='translateX(-50%) rotate('+deg+'deg)';let text='Follow the arrow';if(dist<8)text=m.type==='combat'||m.type==='boss'?'TARGET AREA — eliminate the enemies':'CHECKPOINT REACHED';else if(Math.abs(deg)>115)text='Turn around';else if(Math.abs(deg)>35)text=deg<0?'Turn left':'Turn right';else text='Go straight';hint.textContent=text+' • '+Math.round(dist)+'m'}
setInterval(update,120);
